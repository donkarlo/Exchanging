{% extends 'base.html.twig' %}

{% block body %}
    <h1>Paramhtmlinputtype</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ paramHtmlInputType.id }}</td>
            </tr>
            <tr>
                <th>Monitoring</th>
                <td>{{ paramHtmlInputType.monitoring }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('param-html-input-type_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('param-html-input-type_edit', { 'id': paramHtmlInputType.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
